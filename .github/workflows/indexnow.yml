name: IndexNow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  indexnow:
    # This workflow belongs to the upstream project's website. Forks must not
    # submit indexing notifications for nikki.ranaxro.com.
    if: github.repository == 'RanAxro/nikki_albums'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: IndexNow Action
        uses: bojieyang/indexnow-action@v2
        with:
          sitemap-location: "https://nikki.ranaxro.com/sitemap.xml"
          endpoint: "www.bing.com"
          key: "0d383df4f2444c4eaf00f5d2931df6ef"
          key-location: "https://nikki.ranaxro.com/0d383df4f2444c4eaf00f5d2931df6ef.txt"
          since: 1
          since-unit: day
          lastmod-required: false
